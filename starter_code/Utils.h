#ifndef ASSIGN2_UTILS_H
#define ASSIGN2_UTILS_H

#include "TileCodes.h"
#include <iostream>

class Utils {
    public:
        std::string colourstring(std::string s);
        std::string unicodestring(std::string s);
        std::string unicodecolourstring(std::string s);
    private:
};

#endif // ASSIGN2_UTILS_H